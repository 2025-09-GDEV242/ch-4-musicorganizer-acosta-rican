import java.util.ArrayList;
import java.util.Random; // import random library package to satisfy 4.43, book calls the .nextInt() method.
import java.util.Iterator; // import Iterator libray to test iterator syntax.
import java.util.Collections; // import Collections to access different .Collection API methods
import java.util.Iterator; // import Iterator library to use it for comprehensibility

/**
 * A class to hold details of audio tracks.
 * Individual tracks may be played.
 * 
 * @author David J. Barnes and Michael Kölling
 * @version 2016.02.29
 */
public class MusicOrganizer
{
    /* LAB 4 - MUSIC PLAYER
     * 4.43 - play a random track (think of this is as the now ancient shake your iPod feature to get a random track)
     * 4.45 - write a method to randomly play every track in a list exactly once (a non repeat shuffle feature)
     * (search for the Random object in the Java API to find a method to give you a random number)
     */

    // An ArrayList for storing music tracks.
    private ArrayList<Track> tracks;
    // A player for the music tracks.
    private MusicPlayer player;
    // A reader that can read music files and load them as tracks.
    private TrackReader reader;

    /**
     * Create a MusicOrganizer
     */
    public MusicOrganizer()
    {
        tracks = new ArrayList<>();
        player = new MusicPlayer();
        reader = new TrackReader();
        readLibrary("../audio");
        System.out.println("Music library loaded. " + getNumberOfTracks() + " tracks.");
        System.out.println();
    }
    
    // My work:

    // Testing out loops
    public void listAllFiles() // return type void, doesn't return, just println() commands.
    {
        // testing out for-each loop
        for (Track track : tracks) { // for each element 'track' of type/class 'Track' in collection 'tracks'
            System.out.println(track.getDetails()); //method inheritance .getDetails from class 'Track'
        }
    }
    
    // Testing out iterator
    // had to import java.util.Iterator for hasNext() method.
    public void listAllFilesIterator(){
        Iterator<Track> it = tracks.iterator(); //
        while(it.hasNext()) {
            // Track t = it.next();
            System.out.println(it.next().getDetails());
        }
    }
    
    // LAB # 4 = Play a Random Track (4.43)
    /* 
     * Create a method that will play a random track based on the amount of elements in the arrayList<tracks>
     */
    public void playRandomTrack(){
        Random randomTrack = new Random(); // doable from importing 'Random'
        int randomIndex = randomTrack.nextInt(tracks.size());
        playTrack(randomIndex);
    }
    
    /**
     * Add a track file to the collection.
     * @param filename The file name of the track to be added.
     */
    public void addFile(String filename)
    {
        tracks.add(new Track(filename));
    }
    
    /**
     * Add a track to the collection.
     * @param track The track to be added.
     */
    public void addTrack(Track track)
    {
        tracks.add(track);
    }
    
    /**
     * Play a track in the collection.
     * @param index The index of the track to be played.
     */
    public void playTrack(int index)
    {
        if(indexValid(index)) {
            Track track = tracks.get(index);
            player.playSample(track.getFilename());
            System.out.println("Now playing: " + track.getArtist() + " - " + track.getTitle());
        }
    }
    
    /**
     * Return the number of tracks in the collection.
     * @return The number of tracks in the collection.
     */
    public int getNumberOfTracks()
    {
        return tracks.size();
    }
    
    /**
     * List a track from the collection.
     * @param index The index of the track to be listed.
     */
    public void listTrack(int index)
    {
        System.out.print("Track " + index + ": ");
        Track track = tracks.get(index);
        System.out.println(track.getDetails());
    }
    
    /**
     * Show a list of all the tracks in the collection.
     */
    public void listAllTracks()
    {
        System.out.println("Track listing: ");

        for(Track track : tracks) {
            System.out.println(track.getDetails());
        }
        System.out.println();
    }
    
    /**
     * List all tracks by the given artist.
     * @param artist The artist's name.
     */
    public void listByArtist(String artist)
    {
        for(Track track : tracks) {
            if(track.getArtist().contains(artist)) {
                System.out.println(track.getDetails());
            }
        }
    }
    
    /**
     * Remove a track from the collection.
     * @param index The index of the track to be removed.
     */
    public void removeTrack(int index)
    {
        if(indexValid(index)) {
            tracks.remove(index);
        }
    }
    
    /**
     * Play the first track in the collection, if there is one.
     */
    public void playFirst()
    {
        if(tracks.size() > 0) {
            player.startPlaying(tracks.get(0).getFilename());
        }
    }
    
    /**
     * Stop the player.
     */
    public void stopPlaying()
    {
        player.stop();
    }

    /**
     * Determine whether the given index is valid for the collection.
     * Print an error message if it is not.
     * @param index The index to be checked.
     * @return true if the index is valid, false otherwise.
     */
    private boolean indexValid(int index)
    {
        // The return value.
        // Set according to whether the index is valid or not.
        boolean valid;
        
        if(index < 0) {
            System.out.println("Index cannot be negative: " + index);
            valid = false;
        }
        else if(index >= tracks.size()) {
            System.out.println("Index is too large: " + index);
            valid = false;
        }
        else {
            valid = true;
        }
        return valid;
    }
    
    private void readLibrary(String folderName)
    {
        ArrayList<Track> tempTracks = reader.readTracks(folderName, ".mp3");

        // Put all thetracks into the organizer.
        for(Track track : tempTracks) {
            addTrack(track);
        }
    }
}
